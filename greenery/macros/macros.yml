version: 2

macros:
  - name: non_negative
    description: Test to see if values in column are >= 0.
    arguments:
    - name: dbt_model
      type: dbt_model
    - name: column_name
      type: string